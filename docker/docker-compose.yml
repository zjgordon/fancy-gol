name: fancy-gol

services:
  app:
    image: fancy-gol:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - '8080:8080'
    environment:
      - PORT=8080
    restart: unless-stopped
